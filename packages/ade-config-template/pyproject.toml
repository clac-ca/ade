[project]
name = "ade-config-template"
version = "0.1.0"
description = "Template ADE config package for row, column, and normalization logic"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["uv_build>=0.9.28,<0.10.0"]
build-backend = "uv_build"
