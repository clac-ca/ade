name: Deployment Pipeline

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

env:
  ADE_NODE_VERSION: "22.12.0"
  ADE_PNPM_VERSION: "10.30.1"

permissions: {}

jobs:
  commit_stage:
    name: Commit Stage
    runs-on: ubuntu-latest
    timeout-minutes: 25
    permissions:
      contents: read
      packages: write
    outputs:
      image: ${{ steps.publish_release_candidate.outputs.image }}
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Set up pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v4
        with:
          version: ${{ env.ADE_PNPM_VERSION }}

      - name: Set up Node
        uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6.3.0
        with:
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml
          node-version: ${{ env.ADE_NODE_VERSION }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@4d04d5d9486b7bd6fa91e7baf45bbb4f8b9deedd # v4.0.0

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm lint

      - name: Unit tests
        run: pnpm test:unit

      - name: Build release candidate
        run: pnpm build

      - name: Log in to GitHub Container Registry
        if: github.event_name == 'push'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: echo "$GITHUB_TOKEN" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin

      - name: Publish release candidate
        id: publish_release_candidate
        if: github.event_name == 'push'
        run: |
          owner="$(echo "${GITHUB_REPOSITORY_OWNER}" | tr '[:upper:]' '[:lower:]')"
          image="ghcr.io/${owner}/ade:sha-${GITHUB_SHA}"

          docker tag ade:local "${image}"
          docker push "${image}"

          echo "image=${image}" >> "${GITHUB_OUTPUT}"

  acceptance_stage:
    name: Acceptance Stage
    if: github.event_name == 'push'
    needs:
      - commit_stage
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
    env:
      ADE_BASE_URL: http://127.0.0.1:4100
      ADE_SQL_SA_PASSWORD: "AdeAcceptance1!"
      AZURE_SQL_CONNECTIONSTRING: >-
        Server=sqlserver,1433;Database=ade;User Id=sa;Password=AdeAcceptance1!;Encrypt=false;TrustServerCertificate=true
      AZURE_STORAGEBLOB_CONNECTIONSTRING: >-
        DefaultEndpointsProtocol=http;AccountName=devstoreaccount1;AccountKey=Eby8vdM02xNOcqFeqCnf2fV4i+7VQ8Jtq/K1SZFPTOtr/KBHBeksoGMGwP/IQ+J4MJBxRcQ6vL6gE0Gv6hA==;BlobEndpoint=http://azurite:10000/devstoreaccount1;QueueEndpoint=http://azurite:10001/devstoreaccount1;TableEndpoint=http://azurite:10002/devstoreaccount1
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Set up pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v4
        with:
          version: ${{ env.ADE_PNPM_VERSION }}

      - name: Set up Node
        uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6.3.0
        with:
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml
          node-version: ${{ env.ADE_NODE_VERSION }}

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Pull release candidate
        run: docker pull "${{ needs.commit_stage.outputs.image }}"

      - name: Start acceptance dependencies
        run: docker compose -p ade-acceptance up -d --wait azurite sqlserver

      - name: Run acceptance migrations
        run: >
          docker run --rm
          --network ade-acceptance_default
          --env AZURE_SQL_CONNECTIONSTRING
          "${{ needs.commit_stage.outputs.image }}"
          node dist/migrate.js

      - name: Start release candidate
        run: |
          docker rm --force ade-acceptance-app 2>/dev/null || true
          docker run --detach --rm \
            --name ade-acceptance-app \
            --network ade-acceptance_default \
            --publish 4100:8000 \
            --env AZURE_SQL_CONNECTIONSTRING \
            --env AZURE_STORAGEBLOB_CONNECTIONSTRING \
            "${{ needs.commit_stage.outputs.image }}"

      - name: Wait for release candidate
        run: |
          timeout 120 bash -c 'until curl -fsS http://127.0.0.1:4100/api/readyz; do sleep 1; done'

      - name: Run acceptance tests
        run: pnpm test:acceptance

      - name: Dump acceptance container logs
        if: failure()
        run: |
          docker logs ade-acceptance-app || true
          docker compose -p ade-acceptance logs --no-color azurite sqlserver || true

      - name: Stop acceptance environment
        if: always()
        run: |
          docker rm --force ade-acceptance-app || true
          docker compose -p ade-acceptance down -v --remove-orphans || true

  release_stage:
    name: Release Stage
    if: github.event_name == 'push'
    needs:
      - commit_stage
      - acceptance_stage
    runs-on: ubuntu-latest
    timeout-minutes: 45
    environment:
      name: production
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Set up Node
        uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6.3.0
        with:
          node-version: ${{ env.ADE_NODE_VERSION }}

      - name: Log in to Azure
        uses: azure/login@a457da9ea143d694b1b9c7c869ebb04ebe844ef5 # v2
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}

      - name: Deploy accepted release candidate
        id: deploy_release_candidate
        env:
          ADE_IMAGE: ${{ needs.commit_stage.outputs.image }}
        run: |
          migration_job_name="$(
            az deployment group create \
              --name "ade-production-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}" \
              --resource-group "${{ vars.AZURE_RESOURCE_GROUP }}" \
              --parameters infra/environments/main.prod.bicepparam \
              --query properties.outputs.migrationJobName.value \
              --output tsv
          )"

          echo "migration_job_name=${migration_job_name}" >> "${GITHUB_OUTPUT}"

      - name: Run database migrations
        env:
          ADE_MIGRATION_JOB_NAME: ${{ steps.deploy_release_candidate.outputs.migration_job_name }}
        run: |
          execution_name="$(
            az containerapp job start \
              --name "${ADE_MIGRATION_JOB_NAME}" \
              --resource-group "${{ vars.AZURE_RESOURCE_GROUP }}" \
              --query name \
              --output tsv
          )"

          echo "Started migration job execution: ${execution_name}"

          for attempt in $(seq 1 90); do
            status="$(
              az containerapp job execution list \
                --name "${ADE_MIGRATION_JOB_NAME}" \
                --resource-group "${{ vars.AZURE_RESOURCE_GROUP }}" \
                --query "[?name=='${execution_name}'] | [0].properties.status" \
                --output tsv
            )"

            if [ "${status}" = "Succeeded" ]; then
              echo "Migration job succeeded."
              exit 0
            fi

            if [ "${status}" = "Failed" ]; then
              echo "Migration job failed."
              az containerapp job execution list \
                --name "${ADE_MIGRATION_JOB_NAME}" \
                --resource-group "${{ vars.AZURE_RESOURCE_GROUP }}" \
                --query "[?name=='${execution_name}'] | [0]" \
                --output json
              exit 1
            fi

            sleep 10
          done

          echo "Timed out waiting for migration job execution ${execution_name}."
          exit 1
